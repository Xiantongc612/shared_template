from utility_scripts.analysis import summarize


def test_summarize_reports_count_and_mean() -> None:
    result = summarize([1.0, 2.0, 3.0, 4.0])

    assert result["count"] == 4.0
    assert result["mean"] == 2.5
